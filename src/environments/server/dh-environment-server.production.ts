import { IDhEnvironment } from '@dh/environments/models/dh-environment';

export const DH_ENVIRONMENT: IDhEnvironment = {
  baseUrl: 'to-define',
  hmr: {
    isEnabled: false
  },
  isProduction: true
};
