/**
 * @todo add coverage
 * Did not find a good way to inject the services with spectator
 */
describe('DhTranslationInitializer', (): void => {
  // tslint:disable-next-line:no-empty
  describe('dhTranslationInitializer()', (): void => {
  });
});
